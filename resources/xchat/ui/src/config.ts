export const CHAT_CONFIG = {
  MAX_VISIBLE_MESSAGES: 120,
  PIN_THRESHOLD_PX: 10,
  ENABLE_INLINE_COLOR_CODES: true, // Enabled, but only for trusted messages from server

  // Auto-hide settings
  AUTO_HIDE_ENABLED: true,
  AUTO_HIDE_DURATION: 5000, // 5 seconds

  // Input limits
  MAX_INPUT_LENGTH: 256,

  // Settings visibility
  ALLOW_PLAYER_SETTINGS: true, // If true, player can manage auto-hide
}
